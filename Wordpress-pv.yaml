kind: PersistentVolume
apiVersion: v1
metadata:
  name: wordpress-pv-volume  # Sets PV's name
  namespace: wordpress-ns
  labels:
    type: local  # Sets PV's type to local
    compute: Application
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi # Sets PV Volume
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
